import {NextResponse} from 'next/server'; import {prisma} from '../../../../lib/prisma'; import {getSession} from '../../../../lib/auth';
export async function POST(req:Request){try{const u=await getSession();if(!u)throw new Error('Unauthorized');const {id}=await req.json();const b=await prisma.booking.findUnique({where:{id}});if(!b)throw new Error('Booking tidak ditemukan');
if('release'==='release' && u.role==='DRIVER')throw new Error('Forbidden'); if('release'==='reject' && (u.role!=='DRIVER'||b.driverId!==u.driverId))throw new Error('Forbidden'); if('release'==='complete' && u.role==='DRIVER'&&b.driverId!==u.driverId)throw new Error('Forbidden'); if('release'==='cancel' && u.role==='DRIVER')throw new Error('Forbidden');
const status='release'==='release'?'RELEASED':'release'==='reject'?'REJECTED':'release'==='cancel'?'CANCELLED':'COMPLETED'; const data:any={status}; if(status==='RELEASED')data.releasedAt=new Date(); const n=await prisma.booking.update({where:{id},data}); await prisma.auditLog.create({data:{bookingId:id,userId:u.id,action:'RELEASE',oldValue:b.status,newValue:status}}); return NextResponse.json(n)}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
