'use client'; import {useRouter} from 'next/navigation';
export default function BookingClient({rows,driver}:any){const r=useRouter();async function act(path:string,id:string){const x=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});if(!x.ok){const j=await x.json();alert(j.error||'Gagal');return}r.refresh()}return <div className="card tablewrap"><table className="table"><thead><tr><th>Date</th><th>Booking</th><th>Hotel</th><th>Customer</th><th>Time</th><th>Vehicle</th><th>Driver</th><th>Type</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map((b:any)=><tr key={b.id}><td>{new Date(b.bookingDate).toLocaleDateString('id-ID')}</td><td><b>{b.bookingNumber}</b></td><td>{b.hotel.name}</td><td>{b.customerName}</td><td>{new Date(b.pickupTime).toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})}–{new Date(b.endTime).toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})}</td><td>{b.vehicle.plateNumber}</td><td>{b.driver.name}</td><td>{b.bookingType}</td><td><span className={'pill '+(b.status==='CONFIRMED'?'ok':b.status==='PENDING_DRIVER'?'warn':b.status==='CANCELLED'||b.status==='REJECTED'?'bad':'')}>{b.status}</span></td><td><div className="actions">{!driver&&b.status==='CONFIRMED'&&<button onClick={()=>act('/api/bookings/release',b.id)}>Release</button>}{!driver&&['PENDING_DRIVER','CONFIRMED'].includes(b.status)&&<button onClick={()=>act('/api/bookings/cancel',b.id)}>Cancel</button>}</div></td></tr>)}</tbody></table></div>}
