'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function DriverClient({rows}:any){const r=useRouter();const [msg,setMsg]=useState('');async function act(path:string,id:string){setMsg('Processing...');const x=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});const j=await x.json();setMsg(x.ok?'Berhasil':j.error||'Gagal');if(x.ok)r.refresh()}return <div className="card tablewrap"><table className="table"><thead><tr><th>Date</th><th>Time</th><th>Booking</th><th>Hotel</th><th>Customer</th><th>Route</th><th>Vehicle</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map((b:any)=><tr key={b.id}><td>{new Date(b.bookingDate).toLocaleDateString('id-ID')}</td><td>{new Date(b.pickupTime).toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})}</td><td>{b.bookingNumber}</td><td>{b.hotel.name}</td><td>{b.customerName}</td><td>{b.pickupLocation} → {b.destination}</td><td>{b.vehicle.plateNumber}</td><td><span className="pill">{b.status}</span></td><td><div className="actions">{b.status==='PENDING_DRIVER'&&<><button onClick={()=>act('/api/bookings/driver-confirm',b.id)}>Confirm</button><button onClick={()=>act('/api/bookings/reject',b.id)}>Reject</button></>}{b.status==='CONFIRMED'&&<button onClick={()=>act('/api/bookings/complete',b.id)}>Complete</button>}</div></td></tr>)}</tbody></table>{msg&&<div className="notice">{msg}</div>}</div>}
