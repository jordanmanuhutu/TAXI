'use client'; import {useEffect,useState} from 'react';
export default function Reports(){const [month,setMonth]=useState(new Date().toISOString().slice(0,7));const [data,setData]=useState<any>(null);useEffect(()=>{fetch('/api/reports/monthly?month='+month).then(r=>r.json()).then(setData)},[month]);return <main className="wrap"><div className="top"><div><h1>Monthly Reports</h1><p className="muted">Rekap order, revenue, driver dan vehicle.</p></div><div className="actions"><input value={month} onChange={e=>setMonth(e.target.value)} type="month"/><a href={`/api/reports/monthly?month=${month}&format=csv`}>Export CSV</a></div></div>{data?.summary&&<div className="grid"><div className="card stat"><div className="label">Orders</div><div className="value">{data.summary.orders}</div></div><div className="card stat"><div className="label">Revenue</div><div className="value">Rp {data.summary.revenue.toLocaleString('id-ID')}</div></div><div className="card stat"><div className="label">Confirmed/Done</div><div className="value">{data.summary.confirmed}</div></div><div className="card stat"><div className="label">Month</div><div className="value">{month}</div></div></div>}<div className="card section tablewrap"><table className="table"><thead><tr><th>Booking</th><th>Date</th><th>Hotel</th><th>Customer</th><th>Vehicle</th><th>Driver</th><th>Type</th><th>Status</th><th>Price</th></tr></thead><tbody>{data?.rows?.map((r:any)=><tr key={r.id}><td>{r.bookingNumber}</td><td>{new Date(r.bookingDate).toLocaleDateString('id-ID')}</td><td>{r.hotel.name}</td><td>{r.customerName}</td><td>{r.vehicle.plateNumber}</td><td>{r.driver.name}</td><td>{r.bookingType}</td><td>{r.status}</td><td>Rp {Number(r.price).toLocaleString('id-ID')}</td></tr>)}</tbody></table></div></main>}
