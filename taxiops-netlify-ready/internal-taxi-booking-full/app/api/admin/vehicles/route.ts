import {NextResponse} from 'next/server'; import {prisma} from '../../../../lib/prisma'; import {getSession} from '../../../../lib/auth';
export async function GET(){const u=await getSession();if(!u||u.role==='DRIVER')return NextResponse.json({error:'Forbidden'},{status:403});const where:any=u.role==='ADMIN'?{hotelId:u.hotelId}:{};return NextResponse.json(await prisma.vehicle.findMany({where,include:{hotel:true},orderBy:{plateNumber:'asc'}}))}
export async function POST(req:Request){const u=await getSession();if(!u)return NextResponse.json({error:'Forbidden'},{status:403});try{const x=await req.json();if(u.role==='ADMIN')x.hotelId=u.hotelId;return NextResponse.json(await prisma.vehicle.create({data:x}))}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
