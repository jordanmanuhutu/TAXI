services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: taxi
      POSTGRES_PASSWORD: taxi
      POSTGRES_DB: taxi_booking
    ports: ["5432:5432"]
    volumes: [taxi_pgdata:/var/lib/postgresql/data]
volumes:
  taxi_pgdata:
